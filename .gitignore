node_modules/
dist/
# Bundle de servidor usado só para gerar o HTML estático no build (ADR-005).
dist-ssr/
.vercel/
.DS_Store
*.log
.env
.env.local

# Configuração local do Claude Code (permissões e MCP da máquina).
# Agentes e skills em .claude/agents e .claude/skills SÃO versionados.
.claude/settings.local.json

# Canvas do manual de identidade: 2,2 MB de código do editor embutido, gerado
# a partir das pranchas .dc.html + canvas.json (que SÃO versionadas). Regerável
# quando preciso; o manual final identidade-visual-v2.html também é versionado.
docs/design/identidade/pegpay-identidade-visual.html
